<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Code Conversations</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <div class="wrapper">
      <nav>
          <a href="/">home</a>
        {{#if loggedIn}}
        <a href="/dashboard">dashboard</a>
        <button id="logout" class="btn-no-style">logout</button>
        {{else}}
        <a href="/login">login</a>
        {{/if}}
      </nav>
    <header>
      <h1>
        <a href="/">Code Conversations</a>
      </h1>
    </header>
    <main>
        {{!-- Syntax for data that will be plugged in later --}}
      {{{ body }}}
    </main>
    {{!-- <footer>
      Thanks for visiting!
    </footer> --}}
  </div>
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>